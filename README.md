# NYC Job Search Pipeline Agent

See [job_search_agent_architecture.md](job_search_agent_architecture.md) for the full design.

## Repo layout

- `config/companies.yaml` — tier-list companies mapped to ATS type + Track (A/B/C). Populated by the company ATS audit (build order step 1).
- `.claude/agents/` — subagent definitions for each pipeline stage (ats-discovery, linkedin-discovery, company-monitor, fit-scorer, resume-tailor, form-filler).
- `src/db/schema.sql` — SQLite schema (postings, filter_results, scores, tailored, applications, company_page_hashes, company_monitor_alerts).
- `src/discovery/`, `src/filter/`, `src/score/`, `src/tailor/`, `src/formfill/`, `src/dashboard/` — pipeline stage implementations, filled in per the build order.
- `resumes/` — existing per-company LaTeX resume bases (baseline tailoring inputs).
- `data/` — local SQLite DB and generated artifacts (gitignored); swept after each live-verification run, treat as scratch, not storage.
- `reviewed_output/` — permanent local copies of tailored resume PDFs/.tex for manual review (gitignored, contains personal info). Unlike `data/`, never touched by cleanup -- copy anything here you need to survive past the current session.

## Running discovery

```
pip install -r requirements-dev.txt   # includes pytest
python3 -m pytest tests/              # unit tests (no network)
python3 -m src.discovery.run_track_a_simple    # live poll: Greenhouse + Ashby companies (39)
python3 -m src.discovery.run_track_a_workday   # live poll: Workday companies (17, paginated)
python3 -m src.discovery.run_track_c_monitor   # live check: hash-diff monitor, 32 custom/protected companies
python3 -m src.discovery.run_track_b_linkedin  # live scrape: Apify LinkedIn jobs actor, needs APIFY_TOKEN
python3 -m src.filter.run_filter               # filter postings: NYC + full-time + SWE/AI title, no LLM
python3 -m src.score.run_score                 # LLM fit score (Haiku 4.5) for filter-passed postings, needs ANTHROPIC_API_KEY
python3 -m src.tailor.run_tailor                # tailor resume + outreach draft for score>=8 postings, needs ANTHROPIC_API_KEY + pdflatex
python3 -m src.tailor.run_review_list           # markdown review list for scored postings below the tailor threshold
python3 -m src.dashboard.run_digest             # one combined markdown digest: ready-to-review + review list + Track C alerts
python3 -m src.dashboard.set_application_status <posting_id> <accepted|rejected>   # the accept mechanism
```

The Track A runners are idempotent — re-running against the same DB only inserts postings not already seen (deduped by URL hash). The Workday runner is slower by design: it paginates in steps of 20 with a delay between pages and retries an empty page before treating it as terminal, per the architecture doc. The Track C runner is a monitor, not a discovery source -- it writes to `company_page_hashes` every run and only writes to `company_monitor_alerts` (a separate table from `postings`) when a hash change looks job-shaped; see `src/discovery/company_monitor.py`'s module docstring for the full design. The Track B runner needs an `APIFY_TOKEN` (in a `.env` file in the repo root, or exported) -- it fails with a clear message rather than silently skipping if the token is missing.

## Status

- Step 1 (company ATS audit) — done, see `config/companies.yaml` (88 companies, 56 Track A / 32 Track C).
- Step 2 (Track A discovery) — done, all three sources:
  - Greenhouse + Ashby: `src/discovery/{greenhouse,ashby}.py` + `run_track_a_simple.py`, 39 companies.
  - Workday: `src/discovery/workday.py` + `run_track_a_workday.py`, 17 companies, paginated POST with empty-page retry. Two undocumented Workday quirks drove the pagination logic (see the module docstring): the `total` field goes unreliable near the end of a board, and an out-of-range offset silently re-serves page 1 instead of coming back empty -- so termination is based on partial-page-size, not on `total` or on emptiness alone.
  - Shared `normalize.py` / `store.py` / `config.py` across all three sources -- no per-source dedup logic duplicated.
  - Tested live end-to-end for both runners (fresh-DB pass + idempotency-check pass), not just unit tests against fixtures.
- Track C (company-monitor hash-diffing) — done: `src/discovery/company_monitor.py` + `run_track_c_monitor.py`, 32 companies. Hashes normalized visible text (not raw HTML, not embedded JS state -- tested and rejected, see module docstring for why), diffs on a hash change and keyword-gates the diff before writing a low-confidence "worth a look" alert to its own table, never straight into `postings`. First run per company seeds a baseline only (no alert). Live-tested end-to-end (fresh pass + repeat pass): 29/32 companies fetch successfully, 3 are bot-blocked (Tesla, Citadel Securities, Meta -- tracked via `consecutive_fetch_failures`, not treated as a content change), 10 flagged `low_confidence` (client-rendered shells where hash-diffing is a weak signal), and one live noise source found and fixed mid-build (a per-request hex token on Phenom-People-backed sites that changed on every fetch regardless of content -- now scrubbed).
- Track B (LinkedIn via Apify) — done: `src/discovery/linkedin.py` + `run_track_b_linkedin.py`, built against the documented input/output schema of the `curious_coder/linkedin-jobs-scraper` actor (checked live via its public docs, not guessed). Reuses `normalize.py`/`store.py` exactly as Track A/C do -- `normalize_linkedin_job` is the only new mapping, tagged `source: "linkedin"`. Adaptive widening: if a search returns fewer than 5 results it escalates through a 4-step ladder (broaden keywords, then radius, then both) before giving up, returning the best attempt found rather than nothing, same retry-with-honest-reporting shape as the Track A/C retry logic. 17 unit tests cover the Apify call shape and every widening branch against mocked responses. Two real bugs surfaced only by the live pass, both fixed and covered by regression tests: (1) Apify's run-sync endpoint returned HTTP 201 (not 200) for a run that actually executed rather than reusing a cached result -- the status check only accepted exactly 200 and treated a perfectly valid response as an error; (2) LinkedIn's `link` URL carries per-search tracking params (`refId`/`trackingId`/`position`) that regenerate on every fresh search of the same query, so hashing the raw link made every re-fetch of an already-seen posting look "new" -- dedup now keys off the stable `id` field instead. Live-verified end-to-end after both fixes: first pass inserted 50 new postings, second pass against the same DB found all 50 already seen, 0 new -- clean idempotency.
- Filter stage — done: `src/filter/rules.py` + `run_filter.py`, four cheap deterministic rules, no LLM calls, checked in order (location → employment_type → title_keyword → title_seniority) so a posting failing several is reported under the first one, not double-counted. Every rule was shaped against real data pulled by the discovery runners, not assumed:
  - **Location**: a strict "New York, NY" substring match would have missed most genuinely-NYC postings -- Greenhouse spells the state out ("New York, New York, United States"), Ashby/Workday often give just the bare city ("New York") with no state at all. Confirmed with you before broadening: now splits multi-location strings on `;` and matches if any entry's city (the text before its first comma) is exactly "New York" -- this also correctly rejects state-reference false positives ("Albany, New York") and treats a posting listing "New York, NY" alongside a separate "Remote" entry as a real NYC opportunity, not a remote-only one (found 29 real postings shaped exactly like that).
  - **Employment type**: only Ashby (982/982) and LinkedIn (50/50) postings carry a structured `employmentType` field live; Greenhouse and Workday's list endpoints expose none at all (0/5833, 0/23) -- defaulting those to "excluded" would have gutted most of Track A for a data-availability reason, not a real employment-type mismatch. Uses the structured field when present, falls back to a title keyword heuristic (catches "... Intern", "... Contract", etc.) when absent, and only then defaults to full-time.
  - **Title**: matches a keyword set scoped to what was asked (Software Engineer/SWE/Software Developer/Backend/Full Stack/Frontend/AI Engineer/Machine Learning Engineer + close variants), word-boundary matched so short tokens like "swe" can't false-positive inside unrelated words ("answered"). Excludes Senior/Sr./Staff/Principal/Manager, plus Lead -- added after reviewing real results showed ~18% of what passed was "Lead Software Engineer"-type titles (mostly Capital One), which reads as senior-tier in practice and is out of scope at ~22 months of experience. Also picked up narrow AI/ML-signal additions after reviewing 25 excluded Data/Research Engineer postings: Deep Learning Engineer, Deep Learning Research Engineer, and bare "Research Engineer" (HRT/Jump Trading-style research-engineer roles skew quant/ML even without the literal word "AI", and this phrase alone also covers "AI Research Engineer" and "Machine Learning Research Engineer" as substrings). Deliberately did NOT add bare "Data Engineer" -- reviewed samples showed it pulls in far more data-infra/pipeline noise (Point72's Data Engineer cluster, Stripe's "Data Analyst") than real ML matches; verified live that Point72's Data Engineer roles stayed excluded after the change while their "Research Engineer, Knowledge Graph Intelligence" role correctly started passing.
  - **Dedup**: a new `filter_results` table (posting_id PK) records pass/fail + reason per posting; a posting already in it, passed or excluded, is never re-evaluated.
  - 52 unit tests against fixtures grounded in the real formats found, then live-run against the full postings table from all three discovery tracks (repeated after each rule change), then a second pass each time confirming 0 newly evaluated / all already filtered. Current result: **117 passed** of 21,327 (19,961 excluded by location, 1,158 by title keyword, 60 by title seniority, 31 by employment type). Of the 23 Data/Research Engineer postings that reached the title check, 10 now pass (all HRT/Jump Trading/PDT Partners/Tower Research AI-or-research-flavored roles) and 13 stay excluded (Point72's Data Engineer cluster, Jane Street/BlackRock/Blackstone/Morgan Stanley/Capital One Data Engineer variants, Stripe's Data Analyst) -- confirmed the narrower keywords pulled in the intended roles without reintroducing the infra/pipeline noise.
- Score stage — done: `src/score/{profile,scorer,store}.py` + `run_score.py`.
  - **Profile**: `profile.py` is a small custom parser for the Baseline resume's LaTeX macros (`\resumeSubheading`, `\resumeItem`, `\resumeProjectHeading`, `\section`) -- not a general LaTeX-to-text converter, scoped to this template specifically since all 8 resumes share the same command definitions. Renders Education/Technical Skills/Professional Experience/AI-ML Projects as clean plain text, in document order, with contact info excluded (not relevant to fit). Baseline was picked over a company-tailored resume deliberately -- diffing it against a couple of variants (Virtu, Optiver) showed they're the same underlying jobs/bullets just re-worded per company, not different experience.
  - **Scorer**: Haiku 4.5, called with a forced tool-use (`submit_fit_score`, `{score: 1-10, reasoning: string}`) rather than asking for free text and regex-parsing it -- deterministic to extract, and `parse_tool_response` rejects an out-of-range score or missing reasoning as an error rather than silently storing garbage. Job descriptions aren't uniformly available -- Greenhouse/Ashby/LinkedIn carry one, Workday's `cxs/jobs` list endpoint exposes none at all -- so the prompt says so explicitly rather than silently omitting the field. The system prompt frames the actual agentic ask from the doc: judge fit holistically rather than trusting title-keyword-matched-therefore-good-fit, since the Filter stage already did the hard keyword filtering and this stage's whole purpose is to catch what that can't (a superficially-matching title that's really a different job in practice, or a seniority mismatch the description implies but the title doesn't state).
  - **Dedup**: only scores postings that passed `filter_results` and don't already have a `scores` row -- mirrors `filter/store.py`'s shape exactly.
  - 30 unit tests (7 profile extraction against a synthetic fixture resume, 23 scorer/prompt/tool-parsing against a mocked Anthropic client), no real API calls in the test suite, then live-run against all 117 filter-passed postings with a second pass confirming 0 newly scored / all already scored. Score distribution: 9:1, 8:25, 7:37, 6:24, 5:4, 4:13, 3:11, 2:2 (mean 6.2). Hand-checked a sample of the reasoning against the actual postings -- consistently specific and grounded in real details on both sides (candidate's actual employers/stack/experience length vs. the posting's actual stated requirements), no generic filler or hallucinated claims found. One case validated the agentic feature directly: BlackRock's "Rust, AI Engineer, Director" posting passed the Filter stage (its title contains "AI Engineer" and "Director" was never in the keyword-based seniority exclusion list), but Haiku correctly scored it 3/10, citing the Director-level seniority mismatch against the candidate's ~1 year of experience -- exactly the kind of judgment call the doc's design intends this stage to make that a keyword list can't.
- Tailor stage — done, live-verified end to end including real PDF compiles: `src/tailor/{resume_tailor,store,review}.py` + `run_tailor.py` + `run_review_list.py`. Threshold: score >= 8 auto-tailors a resume + outreach draft (count moves run to run with normal LLM scoring variance -- 20 on the live run below); everything scored but below that goes into a markdown review list instead (company/title/score/reasoning/link, sorted by score descending). Live-ran the review-list generator: 88 postings, correctly sorted and formatted.
  - **A real resume-tailor skill did turn up**, after an initial miss: a first-pass filename search timed out and got backgrounded without being checked, so the Tailor stage was first built from a self-authored (simplified) prompt. The delayed search result turned up `~/Downloads/resume-tailor.yaml` -- despite the extension, actually a zip archive containing a real `resume-tailor/SKILL.md`: a career-services keyword-categorization methodology (RED = technical skills, BLUE = soft skills, YELLOW = tools/frameworks, GREEN = hard requirements) calling for bullet reordering within each experience, Technical Skills updates with genuinely-known tools, and a "Tailoring Summary" of changes made. Flagged this immediately rather than quietly using the wrong prompt, and rebuilt for full fidelity per explicit direction.
  - **Only structured content -- never raw LaTeX -- is what the LLM touches.** `src/common/latex.py` (shared by Score and Tailor) locates every `\resumeSubheading` / `\resumeProjectHeading` / `\resumeItem` / Technical-Skills-`\item` call's exact position; `extract_blocks()` groups each heading with the bullets that follow it, Sonnet only ever sees and returns bullet text/skill categories/a summary, and the response is spliced back into the *original* document at those exact positions.
  - **Two guardrails enforce "never fabricate" programmatically.** `validate_no_fabricated_skills()` rejects any skill the LLM proposes that doesn't already appear in the original resume's skills or bullet text -- triggered live on the first attempt against Valon's posting (rejected a proposed "Docker" that wasn't anywhere in the source resume), and the retry produced a clean, honest skills list instead. `enforce_coursework_annotations()` auto-restores a dropped `(coursework: ...)` annotation so a reorder can never silently imply production experience the original only claimed as coursework.
  - **Compile-loop**: pdflatex in nonstopmode, up to 3 attempts, each retry given the real failure (actual `!`-prefixed error line, "still N pages," or the named rejected skill) rather than a blind re-roll. `find_pdflatex()` resolves the binary via known install locations as a fallback to PATH, since this harness doesn't persist environment variables between tool calls -- a bare `"pdflatex"` lookup would have silently failed on every single invocation otherwise.
  - **A real, systematic bug was caught by the live run and only by the live run.** The first live pass against all 20 score>=8 postings failed 19/20 with an identical LaTeX error ("missing \item"). All 84 mocked unit tests passed throughout -- they verified the *content* the compile-loop wrote was correct, but not that the surrounding LaTeX structure it was spliced into stayed valid. The bug: `extract_skills_block()`'s span covers the *entire* `\item{...}` call, but the splice replacement wasn't re-wrapping its content back inside `\item{...}` -- so the rebuilt document had a `\textbf{...}` sitting directly inside an `itemize` environment with no `\item` command at all. Fixed the splice, added a regression test asserting the `\item{` wrapper survives, and added a new integration test that runs a *real* pdflatex compile against the spliced fixture output (skipped automatically if pdflatex isn't installed) -- exactly the class of bug that only shows up against a real compiler, which the fixture itself also turned out to be missing (it never wrapped its headings in `\resumeSubHeadingListStart`/`End` either, a second, unrelated compile bug in the test fixture caught by the same new test).
  - **Also required real environment troubleshooting beyond the code**: BasicTeX ships a minimal package set. Getting a clean compile took three follow-up `sudo tlmgr install` rounds -- `titlesec enumitem marvosym` plus `preprint` (the actual package name for `fullpage.sty`, corrected after `tlmgr` rejected an invalid guessed name), then `cm-super` (Type 1 CM fonts, after a T1-encoded font lookup failed with no working Metafont pipeline). None of this needed code changes; all confirmed via `tlmgr info`/`kpsewhich` before handing back a next-step command each time.
  - 71 unit tests (36 for the block/skills extraction, fabrication-rejection, coursework-preservation, splicing, compile-loop, and `find_pdflatex` resolution logic against the synthetic fixture resume -- 35 with the Anthropic client and pdflatex both mocked, plus 1 real-compile integration test; 16 for the shared LaTeX helpers; 14 for the review-list/store dedup; 5 for description-extraction).
  - **Live-verified end to end with real PDF compiles**: after the fix, ran twice more against the (run-to-run-varying) score>=8 set -- 20/20, then 29/29 -- **0 failures across both, every one on the first attempt, every one exactly 1 page.** Spot-checked skills lists programmatically (every item traces back to the original resume's skills or bullets -- e.g. "Kafka" and "Drools" correctly validated as genuine since they appear in a bullet, not fabricated) and visually inspected full rendered PDFs -- clean formatting, correct reordering (e.g. AI/ML skills promoted to the top for an "NLP / AI Engineer" posting), all quantified metrics preserved exactly, nothing invented.
  - **`reviewed_output/` at the repo root holds a permanent local copy of every tailored PDF + .tex**, gitignored (contains personal info) and deliberately outside `data/`. This exists because a habitual "clean up test artifacts before committing" step -- fine for the SQLite DB and pytest cache, both pure scaffolding -- got mechanically extended to `data/tailored/*` too and deleted the first real batch of deliverables in the same breath as saying they were ready for review. `data/tailored/` itself still gets swept after each live-verification run like the rest of `data/`; `reviewed_output/` never does, since nothing in the routine cleanup pattern has a reason to touch it.
  - **A manual comparison-report audit across all 29 tailored postings caught something the test suite didn't**: the Tailoring Summary text for Point72's "NLP / AI Engineer" posting claimed *"reordered project blocks so AI Portfolio Agent leads over ML Trading Strategy block"* -- checked the actual document, and the block order was unchanged from baseline. The splice architecture only ever reorders bullets *within* a fixed block; it can't move a whole Experience/Project block relative to another, so that claim described something structurally impossible. Fixed two ways: the `SYSTEM_PROMPT` now explicitly states block order is fixed and instructs the model never to claim otherwise, and `validate_no_block_reorder_claims()` scans every `tailoring_summary` line against a regex set and rejects (triggering a normal retry, same as the fabrication guardrail) any claim of block/section-level reordering. The regex set was checked against all 210 real summary lines from the live 29-posting run before being wired in -- a first pass caught only the one known line; fixing a singular/plural bug in the patterns (`section` didn't match `sections`) surfaced a **second** real bad claim in that same Point72 summary ("Moved Epic and Centene blocks after project blocks..."), confirming the fix generalizes rather than just patching the one observed case. All 210 real lines are checked in the test suite via `tests/fixtures/real_tailoring_summaries.py`, not just hand-written examples.
  - **Live re-verification against Point72's exact posting, after the credit top-up**: re-ran the Tailor stage against Point72's "NLP / AI Engineer" posting (scored 8/10 again, right at the threshold, exactly the case needed) plus the other 8 already-scored postings (PDT Partners, Point72's other variants, Jane Street x3, HRT) without repopulating or rescoring, since that data was already in the DB. All 9 compiled clean on the first attempt; Point72's new Tailoring Summary no longer claims block-level reordering, and `validate_no_block_reorder_claims()` didn't need to reject anything on this run. Fix confirmed. A comparison-report audit across all 29 tailored postings (company/title/leading bullet per section/skills line/summary) separately confirmed real per-posting differentiation, not just cosmetically different phrasing -- spot-checked HRT's AI Research Engineer against DoorDash's entry-level SWE and found genuinely different leading bullets and skills emphasis, not the same content reworded.
  - Tailor stage is fully verified end-to-end as of this point: real PDF compiles, fabrication/coursework/block-reorder guardrails all validated against real model output, and differentiation confirmed by manual audit, not just format-consistency checks.
- Review Dashboard — done: `src/dashboard/{store,digest}.py` + `run_digest.py` + `set_application_status.py`. No new schema and no LLM calls -- pure query + render over what every earlier stage already computed, into one combined `data/dashboard.md`:
  1. **Ready to review** (score >= 8, successfully tailored): company/title/score, fit reasoning, the resume linked into `reviewed_output/`, and the full Tailoring Summary, plus current accept status.
  2. **For manual review** (score < 8): the same content as the standalone review list (reuses `tailor/store.py`'s `get_review_list_postings` directly rather than re-querying), inlined as its own section instead of a separate file, per the "surface three things in one place" ask.
  3. **Company-monitor alerts**, visually and textually set apart (`---` rule, ⚠ heading, explicit "not a confirmed job posting" caveat) so Track C's lower-confidence signal is never mistaken for a real scored posting -- only `status = 'unreviewed'` alerts show, so a dismissed/confirmed one doesn't keep resurfacing in a fresh digest.
  - **Accept mechanism**: reuses the `applications` table that was already in the original schema (`status`: pending_review/accepted/submitted/rejected) rather than adding new schema -- `ensure_application_rows()` gives every successfully-tailored posting a `pending_review` row the first time the digest runs, and never overwrites one a human has already updated. Since this is a generated file, not a live app, "accepting" a posting is a real CLI command (`python3 -m src.dashboard.set_application_status <posting_id> accepted`) printed inline in the digest next to each posting, and its current status renders as a markdown checkbox (`[x] accepted` / `[ ] pending_review`) so the next digest generation reflects what was decided. This is exactly the signal Form-Fill will need to read from later to know what's cleared to act on.
  - **Closes the same gap that caused the earlier `data/tailored/` deletion incident, for this stage specifically**: PDFs only land in `reviewed_output/` today via a manual copy step, so a digest generated right after a Tailor run (before that manual copy happens) would link to files that don't exist yet. `ensure_reviewed_output_copies()` makes the digest responsible for its own linked assets -- it copies anything new from `data/tailored/` into `reviewed_output/` before rendering any links, idempotently (skips files already copied) and gracefully (a source already swept renders as "PDF not found" instead of a dead link, rather than erroring).
  - 33 unit tests against an in-memory DB (row-creation idempotency/never-overwrite-a-human-decision, file-copy idempotency/missing-source handling, join correctness including excluding failed tailoring attempts, all three digest sections' rendering including empty states, and the CLI status-update flow), all passing alongside the full 231-test suite. Live verification against real postings/scores/tailored data is pending: `data/job_pipeline.db` is currently empty (0 bytes) between sessions, so there's no live score>=8/review-list/alert content to check the rendered digest against yet -- confirmed the CLI runs cleanly end-to-end against an empty DB (creates schema, writes a correctly-formatted all-empty-state digest) as a smoke test, but that's not the same as reviewing real rendered output.
