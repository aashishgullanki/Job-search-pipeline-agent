.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.env
data/*.db
data/*.sqlite3
data/review_list.md
data/dashboard.md
data/tailored/*.pdf
data/tailored/*.tex
data/tailored/*.aux
data/tailored/*.log
data/tailored/*.out

# Permanent local copies of tailored resume output for manual review.
# Deliberately OUTSIDE data/ and never touched by any cleanup command --
# data/tailored/* gets swept after each live verification run, this doesn't.
# Contains personal info (name/phone/email), so gitignored, not committed.
reviewed_output/
